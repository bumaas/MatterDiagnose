{
  "name": "mDNS liefert keine Antworten",
  "input": {"mdnsResponses": false},
  "expected": {
    "severities": {"mdns_silent": "blocker"},
    "absent": ["border_router_found", "no_border_router", "own_controller_missing", "own_controller_ok"]
  }
}
